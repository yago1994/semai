{
  "schemaVersion": "1.0.0",
  "fixtures": {
    "outlook-thread": {
      "path": "tests/fixtures/outlook-thread.html",
      "description": "A minimal Outlook Web thread page skeleton for patch testing."
    },
    "outlook-compose": {
      "path": "tests/fixtures/outlook-compose.html",
      "description": "Outlook compose window skeleton used to test compose-area patches."
    }
  },
  "cases": [
    {
      "id": "fixture-thread-structure",
      "description": "Outlook thread fixture has the expected container structure.",
      "fixture": "outlook-thread",
      "patch": null,
      "assertions": [
        {
          "type": "selectorExists",
          "selector": "#ReadingPaneContainerId",
          "message": "Expected #ReadingPaneContainerId in thread fixture."
        },
        {
          "type": "selectorCount",
          "selector": ".ms-MessageList-itemContent",
          "count": 2,
          "message": "Expected 2 message items in thread fixture."
        }
      ]
    },
    {
      "id": "patch-injects-element",
      "description": "A JS patch is executed and stamps a marker attribute on <html>.",
      "fixture": "outlook-thread",
      "patch": "example-noop-v1",
      "assertions": [
        {
          "type": "attributeExists",
          "selector": "html",
          "attribute": "data-semai-patch-example-noop-v1",
          "message": "Expected data-semai-patch-example-noop-v1 attribute on <html> after patch ran."
        }
      ]
    },
    {
      "id": "patch-idempotent",
      "description": "Running the same patch twice does not duplicate the marker attribute.",
      "fixture": "outlook-thread",
      "patch": "example-noop-v1",
      "assertions": [
        {
          "type": "attributeExists",
          "selector": "html",
          "attribute": "data-semai-patch-example-noop-v1",
          "message": "Expected exactly one data-semai-patch-example-noop-v1 marker (idempotency)."
        }
      ]
    },
    {
      "id": "fix-sender-avatar-initials-comma-format",
      "description": "Avatar initials for 'Last, First M.' display name format should use first name initial first (e.g. 'GS' for 'Sabben, Gaelle L.'), not middle name initial.",
      "fixture": "outlook-thread",
      "patch": "fix-sender-avatar-initials-v1",
      "assertions": [
        {
          "type": "selectorExists",
          "selector": "#ReadingPaneContainerId",
          "message": "Expected #ReadingPaneContainerId in thread fixture."
        }
      ]
    }
  ]
}
